import type { NextPage } from 'next';
import Head from 'next/head';

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>BiX</title>
        <meta
          name="description"
          content="Open Source Crypto Exchange Platform"
        />
      </Head>
      <main>index</main>
    </div>
  );
};

export default Home;
